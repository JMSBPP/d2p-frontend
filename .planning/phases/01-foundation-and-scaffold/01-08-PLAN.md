---
phase: 01-foundation-and-scaffold
plan: 08
type: execute
wave: 4
depends_on: ['01-04', '01-07']
files_modified:
  - .github/workflows/ci.yml
  - lighthouserc.cjs
  - tests/visual/anti-patterns.spec.ts
  - tests/visual/fixtures/anti-patterns.html
  - tests/a11y/homepage.spec.ts
  - package.json
  - docs/a11y-audit.md
  - docs/copy-review.md
autonomous: true
requirements:
  - FOUND-07
  - FOUND-08
  - FOUND-09
  - CROSS-01
  - CROSS-03
  - CROSS-04

must_haves:
  truths:
    - "The verified `impeccable detect` CLI flag is documented in `.github/workflows/ci.yml` — the flag name is confirmed live via `npx impeccable detect --help` before the workflow is committed. Workflow uses verified flag OR relies on non-zero exit code."
    - "The CI workflow contains seven parallel jobs: `lint`, `typecheck`, `test-unit`, `test-e2e`, `a11y`, `lighthouse`, `impeccable`. Lighthouse/e2e/a11y run on Vercel `deployment_status` events. Lint/typecheck/test-unit/impeccable run on push and pull_request."
    - "Lighthouse CI enforces LCP ≤ 2500ms and TBT ≤ 200ms on the Moto G Power 3G profile (412×823, DPR 2.625, 1.6Mbps throttling, 4× CPU slowdown). Build fails on regression."
    - "axe-core scan via `@axe-core/playwright` runs against the deployed preview URL with tags `wcag2a wcag2aa wcag21a wcag21aa wcag22aa`. Zero violations required."
    - "A planted-pattern test (`tests/visual/anti-patterns.spec.ts`) confirms impeccable WOULD detect each major anti-pattern (Inter font, purple gradient, eyebrow chip, italic serif h1, nested cards, dark glow) — so the CI job is proven to fire on regressions, not just pass because no violations exist."
    - "`docs/a11y-audit.md` exists with the manual screen-reader audit checklist (CROSS-01 manual half)."
    - "`docs/copy-review.md` exists with the AI-slop copy review checklist (CROSS-10 manual half)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow with 7 parallel jobs and proper deployment_status gating"
    - path: "lighthouserc.cjs"
      provides: "LHCI config with Moto G Power 3G + LCP/TBT budgets"
      contains: "largest-contentful-paint"
    - path: "tests/visual/anti-patterns.spec.ts"
      provides: "Planted-pattern test confirming impeccable detects each anti-pattern"
    - path: "tests/visual/fixtures/anti-patterns.html"
      provides: "Static HTML fixtures intentionally violating impeccable rules"
    - path: "docs/a11y-audit.md"
      provides: "Manual screen-reader audit checklist for top-5-template review"
    - path: "docs/copy-review.md"
      provides: "AI-slop copy review checklist"
  key_links:
    - from: ".github/workflows/ci.yml impeccable job"
      to: "npx impeccable detect"
      via: "shell command with verified flag"
      pattern: "impeccable detect"
    - from: ".github/workflows/ci.yml lighthouse job"
      to: "lighthouserc.cjs"
      via: "lhci autorun reads config"
      pattern: "lhci autorun"
    - from: "tests/a11y/homepage.spec.ts"
      to: "@axe-core/playwright AxeBuilder"
      via: "import + withTags"
      pattern: "wcag22aa"
---

<objective>
Land the CI quality matrix: GitHub Actions workflow with seven parallel jobs covering lint, typecheck, unit tests, e2e tests, accessibility scan, Lighthouse performance budget, and impeccable anti-pattern detection. The most consequential task is verifying the EXACT `impeccable detect` flag name BEFORE committing the CI YAML — RESEARCH.md and CONTEXT.md disagreed on `--fail-on-error` vs `--fail-on-issues`, so the executor MUST run `npx impeccable detect --help` and use whichever flag the CLI actually exposes (or fall back to exit-code-only enforcement). Plant adversarial test fixtures that violate each anti-pattern so the impeccable CI job is proven to detect, not just pass by coincidence.

Purpose: FOUND-07, FOUND-08, FOUND-09, CROSS-01, CROSS-03, CROSS-04 are ALL CI gates. The whole point of Phase 1 is that no downstream phase can ship a regression past the gates established here. The gates must be required for merge from PR #1 — no "warn-only" warm-up.

Output: CI workflow with all 7 jobs; Lighthouse config; impeccable-proof planted-pattern fixtures; manual audit checklists for the two non-automatable cross-cutting requirements (CROSS-01 manual screen-reader, CROSS-10 copy review).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md
@.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md
@.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-and-scaffold/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-scaffold/01-04-SUMMARY.md
@.planning/phases/01-foundation-and-scaffold/01-07-SUMMARY.md

<interfaces>
<!-- impeccable CLI — flag name is the LOW-confidence open question from RESEARCH.md.
     Task 1 below resolves this by running `npx impeccable detect --help` and using
     whichever flag the CLI actually exposes. -->

<!-- Lighthouse Moto G Power emulation values (from RESEARCH.md Pattern 10) -->
- formFactor: mobile
- screenEmulation: { width: 412, height: 823, deviceScaleFactor: 2.625, mobile: true }
- throttling: { rttMs: 150, throughputKbps: 1638.4, cpuSlowdownMultiplier: 4 }
- Budgets: LCP ≤ 2500ms (error), TBT ≤ 200ms (error), CLS ≤ 0.1 (warn)

<!-- axe-core tags for WCAG 2.2 AA (Pattern 11 in RESEARCH.md) -->
.withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify impeccable CLI flag + build planted-pattern fixtures + finalize anti-pattern test</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md (Open Question 1: impeccable CLI exact flag for CI failure; Common Pitfall 4: impeccable CLI Flag Name Uncertainty)
    - /home/jmsbpp/apps/d2p/frontend/.planning/research/PITFALLS.md (Pitfalls 1-15: every impeccable anti-pattern detector — Inter, purple gradient, nested cards, side-tab accent, icon tile, bounce easing, gray on color, pure black, hero metric, gradient text, identical card grids, slop copy, italic serif hero, eyebrow chip, dark glow)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md (FOUND-07, CROSS-04 rows)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md (CI architecture; impeccable detect --fail-on-error)
    - tests/visual/anti-patterns.spec.ts (Wave 0 stub file from Plan 01)
  </read_first>
  <action>
    1. **FIRST AND BEFORE ANY YAML EDIT:** run the live CLI help:
       ```bash
       npx --yes impeccable detect --help 2>&1 | tee /tmp/impeccable-help.txt
       ```
       Inspect the output. Look for one of: `--fail-on-error`, `--fail-on-issues`, `--ci`, or similar. Record the verified flag in `docs/impeccable-flag.md` (one-line note: "Verified flag is `--<x>` as of <date>"). If NO such flag exists, the fallback is to rely on the CLI's non-zero exit code (most CLIs exit non-zero when violations are detected).

       If `npx --yes impeccable` cannot resolve (package not on npm registry / different package name), document the resolution: search for the package via `npm search impeccable detect` or check if installation requires `@pbakaus/impeccable` or similar scoped name. Update `package.json` `devDependencies` to add the correct package name as a dev dep if necessary so CI doesn't depend on a one-shot `npx --yes` network call.

    2. Plant adversarial fixtures at `tests/visual/fixtures/anti-patterns.html` — a static HTML file deliberately triggering each detectable rule:
       ```html
       <!DOCTYPE html>
       <html>
       <head>
         <style>
           body { font-family: 'Inter', sans-serif; background: #000000; }
           .hero { background: linear-gradient(135deg, #7c3aed, #2563eb); }
           h1 { font-family: 'Fraunces'; font-style: italic; font-size: 64px;
                background: linear-gradient(90deg, #f43f5e, #8b5cf6);
                -webkit-background-clip: text; }
           .eyebrow { border-radius: 9999px; letter-spacing: 0.15em;
                      text-transform: uppercase; font-size: 11px; }
           .card { border: 1px solid #ddd; border-radius: 8px; padding: 16px;
                   box-shadow: 0 0 80px rgba(124, 58, 237, 0.4); }
           .card .card { /* nested card — anti-pattern */ }
           .accent-side { border-left: 4px solid #7c3aed; border-radius: 8px; }
           .gray-on-color { background: #e0e7ff; color: #6b7280; }
         </style>
       </head>
       <body>
         <span class="eyebrow">RESEARCH · FINANCE</span>
         <h1>Empower your financial future</h1>
         <div class="hero"></div>
         <div class="card"><div class="card">Nested</div></div>
         <div class="accent-side"></div>
         <p class="gray-on-color">Hard to read</p>
       </body>
       </html>
       ```
       This fixture is read by `tests/visual/anti-patterns.spec.ts` — NOT by the impeccable CI job. The CI job runs against `app/` not against the fixtures directory.

    3. Update `tests/visual/anti-patterns.spec.ts` to be a Vitest or Playwright test that invokes impeccable against the fixture file and asserts violations are reported. Pseudocode:
       ```typescript
       import { execSync } from 'node:child_process'
       import { test, expect } from 'vitest'

       test('impeccable detects each planted anti-pattern in fixtures', () => {
         let stdout = ''
         try {
           stdout = execSync('npx impeccable detect tests/visual/fixtures/anti-patterns.html', { encoding: 'utf8' })
         } catch (err: any) {
           stdout = (err.stdout ?? '') + (err.stderr ?? '')
         }
         // Assert findings include each anti-pattern category we planted
         expect(stdout).toMatch(/inter|overused-font/i)
         expect(stdout).toMatch(/purple|gradient|ai-color-palette/i)
         expect(stdout).toMatch(/eyebrow|icon-tile-stacked-above-heading|eyebrow-chip/i)
         expect(stdout).toMatch(/italic-serif|oversized-italic-serif/i)
         expect(stdout).toMatch(/nested-cards|wrapping-everything-in-cards/i)
         expect(stdout).toMatch(/gray-text-on-colored-background/i)
         expect(stdout).toMatch(/pure-black-background/i)
         expect(stdout).toMatch(/dark-glow|box-shadow.*blur/i)
       })
       ```
       **Note:** If impeccable doesn't expose stdout in this shape, adjust the assertion strategy to: assert non-zero exit code AND parse the JSON report if `--format json` is available. The point is to FAIL when impeccable does not fire on these fixtures.

    4. Run the test: `pnpm vitest run tests/visual/anti-patterns.spec.ts` — must pass (i.e., impeccable DID detect the planted anti-patterns).

    5. The `app/` directory (homepage from Plan 04) should pass impeccable cleanly. Run `npx impeccable detect app/` and confirm zero violations. If violations exist, this is a Plan 04 regression — STOP and report back, do not silently rewrite the homepage.
  </action>
  <verify>
    <automated>pnpm vitest run tests/visual/anti-patterns.spec.ts && npx impeccable detect app/</automated>
    Plus structural:
    - `test -f tests/visual/fixtures/anti-patterns.html`
    - `grep -q "Inter" tests/visual/fixtures/anti-patterns.html`
    - `grep -q "#7c3aed" tests/visual/fixtures/anti-patterns.html`
    - `grep -q "border-left" tests/visual/fixtures/anti-patterns.html`
    - `test -f docs/impeccable-flag.md`  (records verified flag)
  </verify>
  <acceptance_criteria>
    - `docs/impeccable-flag.md` documents the live-verified flag name (or fallback strategy).
    - `tests/visual/fixtures/anti-patterns.html` contains intentional violations for at least 8 distinct impeccable rules.
    - `tests/visual/anti-patterns.spec.ts` invokes impeccable against the fixture and asserts violations are reported.
    - `npx impeccable detect app/` returns 0 violations on the real homepage.
    - Maps to 01-VALIDATION.md FOUND-07, CROSS-04 rows.
  </acceptance_criteria>
  <done>Impeccable flag verified; planted fixtures prove the detector fires; real homepage passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author lighthouserc.cjs + axe-core homepage test</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md (Pattern 10: Lighthouse CI Config for Moto G Power 3G; Pattern 11: axe-core/playwright Integration)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md (Lighthouse profile: Moto G Power 3G; LCP < 2.5s; TBT < 200ms)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md (FOUND-08, FOUND-09, CROSS-01, CROSS-03 rows)
    - /home/jmsbpp/apps/d2p/frontend/.planning/research/PITFALLS.md (Pitfall 30: LCP > 2.5s on 3G Android)
    - tests/a11y/homepage.spec.ts (stub from Plan 01)
    - lighthouserc.cjs (stub from Plan 01 — may need refinement)
  </read_first>
  <action>
    1. Install (or verify) `@lhci/cli` and `@axe-core/playwright`:
       ```bash
       pnpm add -D @lhci/cli@^0.15 @axe-core/playwright@^4.11
       ```

    2. Author `lighthouserc.cjs` with Moto G Power 3G profile and budgets — use the exact values from RESEARCH.md Pattern 10:
       ```javascript
       module.exports = {
         ci: {
           collect: {
             numberOfRuns: 3,
             url: [process.env.LHCI_COLLECT__URL ?? 'http://localhost:3000'],
             settings: {
               formFactor: 'mobile',
               screenEmulation: {
                 mobile: true,
                 width: 412,
                 height: 823,
                 deviceScaleFactor: 2.625,
                 disabled: false,
               },
               throttling: {
                 rttMs: 150,
                 throughputKbps: 1638.4,
                 cpuSlowdownMultiplier: 4,
                 requestLatencyMs: 562.5,
                 downloadThroughputKbps: 1474.6,
                 uploadThroughputKbps: 675,
               },
               throttlingMethod: 'simulate',
             },
           },
           assert: {
             assertions: {
               'largest-contentful-paint': ['error', { maxNumericValue: 2500 }],
               'total-blocking-time':      ['error', { maxNumericValue: 200 }],
               'cumulative-layout-shift':  ['warn',  { maxNumericValue: 0.1 }],
               'first-contentful-paint':   ['warn',  { maxNumericValue: 2000 }],
               'categories:accessibility': ['warn',  { minScore: 0.9 }],
               'categories:performance':   ['warn',  { minScore: 0.75 }],
             },
           },
           upload: { target: 'temporary-public-storage' },
         },
       }
       ```

    3. Update `tests/a11y/homepage.spec.ts` (the Plan 01 stub) to scan the real homepage with WCAG 2.2 AA tags:
       ```typescript
       import { test, expect } from '@playwright/test'
       import AxeBuilder from '@axe-core/playwright'

       test('homepage has no WCAG 2.2 AA violations', async ({ page }) => {
         await page.goto('/')
         const results = await new AxeBuilder({ page })
           .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'])
           .analyze()
         expect(results.violations).toEqual([])
       })

       test('homepage in en locale has no WCAG 2.2 AA violations', async ({ page, context }) => {
         await context.addCookies([{ name: 'NEXT_LOCALE', value: 'en', url: 'http://localhost:3000' }])
         await page.goto('/')
         const results = await new AxeBuilder({ page })
           .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'])
           .analyze()
         expect(results.violations).toEqual([])
       })
       ```
       Both locales must pass since they may render different text widths affecting overflow.

    4. Add npm scripts to `package.json`:
       ```json
       {
         "scripts": {
           "test:a11y":        "playwright test tests/a11y/",
           "test:lighthouse":  "lhci autorun",
           "test:impeccable":  "impeccable detect app/"
         }
       }
       ```

    5. Run locally (against the dev server):
       - `pnpm dev &` then `pnpm test:a11y` — should pass.
       - `pnpm dev &` then `LHCI_COLLECT__URL=http://localhost:3000 pnpm test:lighthouse` — LCP should fit under 2500ms.
       If LCP fails locally, this is a Plan 02/04 regression — investigate the homepage before tweaking the budget.
  </action>
  <verify>
    <automated>pnpm test:a11y</automated>
    Plus structural:
    - `test -f lighthouserc.cjs`
    - `grep -q "largest-contentful-paint" lighthouserc.cjs && grep -q "2500" lighthouserc.cjs`
    - `grep -q "wcag22aa" tests/a11y/homepage.spec.ts`
    - `grep -q "test:lighthouse\\|test:a11y\\|test:impeccable" package.json`
  </verify>
  <acceptance_criteria>
    - `lighthouserc.cjs` exists with Moto G Power 3G emulation and LCP/TBT error budgets.
    - `tests/a11y/homepage.spec.ts` scans both locales with the five WCAG tags.
    - `pnpm test:a11y` passes against the dev server.
    - `pnpm test:lighthouse` against local dev server produces a passing run (or run fails informatively if homepage is too heavy).
    - Maps to 01-VALIDATION.md FOUND-08, FOUND-09, CROSS-01, CROSS-03 rows.
  </acceptance_criteria>
  <done>Lighthouse config + axe-core test green against the real homepage; budgets enforced locally before CI.</done>
</task>

<task type="auto">
  <name>Task 3: Write .github/workflows/ci.yml with 7 parallel jobs + manual audit checklists</name>
  <read_first>
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-RESEARCH.md (Pattern 12: GitHub Actions CI Architecture)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-CONTEXT.md (CI architecture decisions; all jobs required for merge)
    - /home/jmsbpp/apps/d2p/frontend/.planning/phases/01-foundation-and-scaffold/01-VALIDATION.md (CROSS-01 manual audit; CROSS-10 manual copy review)
    - docs/impeccable-flag.md (just created in Task 1)
    - lighthouserc.cjs (just created in Task 2)
  </read_first>
  <action>
    1. Author `.github/workflows/ci.yml` with seven jobs. The first four run on push/PR; the last three run on `deployment_status` (Vercel preview ready):
       ```yaml
       name: CI

       on:
         push:
           branches: [main]
         pull_request:
         deployment_status:

       jobs:
         lint:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm biome check .

         typecheck:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm velite build  # Required so iterations types resolve
             - run: pnpm tsc --noEmit

         test-unit:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm velite build
             - run: pnpm vitest run

         impeccable:
           if: github.event_name != 'deployment_status'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             # NOTE: flag verified live in Task 1 — see docs/impeccable-flag.md.
             # Replace <VERIFIED_FLAG> with the exact value from docs/impeccable-flag.md.
             # If no flag exists, rely on exit code: `npx impeccable detect app/` and `set -e`.
             - run: npx impeccable detect app/ <VERIFIED_FLAG>

         test-e2e:
           if: github.event_name == 'deployment_status' && github.event.deployment_status.state == 'success'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm playwright install --with-deps chromium
             - run: pnpm playwright test --project=chromium
               env:
                 PLAYWRIGHT_TEST_BASE_URL: ${{ github.event.deployment_status.target_url }}

         a11y:
           if: github.event_name == 'deployment_status' && github.event.deployment_status.state == 'success'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm playwright install --with-deps chromium
             - run: pnpm playwright test tests/a11y/
               env:
                 PLAYWRIGHT_TEST_BASE_URL: ${{ github.event.deployment_status.target_url }}

         lighthouse:
           if: github.event_name == 'deployment_status' && github.event.deployment_status.state == 'success'
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version-file: '.nvmrc' }
             - uses: pnpm/action-setup@v4
               with: { version: 9 }
             - run: pnpm install --frozen-lockfile
             - run: pnpm dlx @lhci/cli@^0.15 autorun
               env:
                 LHCI_BUILD_CONTEXT__CURRENT_HASH: ${{ github.sha }}
                 LHCI_COLLECT__URL: ${{ github.event.deployment_status.target_url }}
       ```
       Replace `<VERIFIED_FLAG>` with the actual flag from docs/impeccable-flag.md. If the verified flag is `--fail-on-error`, the command becomes `npx impeccable detect app/ --fail-on-error`. If no flag is supported, drop the placeholder (the command relies on exit code).

    2. Author `docs/a11y-audit.md` with the manual screen-reader audit checklist (CROSS-01 manual half):
       ```markdown
       # Manual Accessibility Audit — Top 5 Templates

       This checklist supplements the automated axe-core CI gate. Manual screen-reader audits
       are required for the top 5 page templates by traffic / agent importance.

       ## Phase 1 — Stub Homepage (`/`)

       Run with VoiceOver (macOS) or NVDA (Windows).

       - [ ] Heading order is logical (h1 then h2; no skipped levels).
       - [ ] Language switcher region is announced ("Language navigation" or similar).
       - [ ] LanguageSwitcher buttons announce active state via aria-current="true".
       - [ ] StatusPill text labels (PASS / FAIL / etc.) are announced — NOT just icons.
       - [ ] Tab order matches visual order: header → main → footer.
       - [ ] Focus ring visible on every interactive element.
       - [ ] Page renders correctly when font size scaled to 200%.

       Record findings inline (date, reviewer, result).

       ## Phase 2 — Iteration Catalog (`/iterations`)
       *To be filled by Phase 2 reviewer.*

       ## Phase 2 — Iteration Detail (`/iterations/<slug>/v<n>`)
       *To be filled by Phase 2 reviewer.*

       ## Phase 3 — Dashboard (`/dashboard`)
       *To be filled by Phase 3 reviewer.*

       ## Phase 5 — Instrument Detail (`/instruments/<id>/<chain>`)
       *To be filled by Phase 5 reviewer.*
       ```

    3. Author `docs/copy-review.md` with the AI-slop copy review checklist (CROSS-10 manual half):
       ```markdown
       # Copy Review — Author Quality, Not AI Slop

       Before any new copy ships, run this manual check. Banned phrasing is also
       caught by impeccable's copy detector, but human review remains required
       for tonal judgment.

       ## Banned phrases (zero tolerance)
       - "empower [audience]"
       - "revolutionize"
       - "transform your X"
       - "seamless"
       - "unlock"
       - "next-gen", "cutting-edge"
       - "leverage your"
       - "unleash"
       - "best-in-class"
       - "[N]% increase in [vanity metric]" without source citation

       ## Tone check
       - [ ] First-person plural ("We measured...") for lab voice.
       - [ ] Specific claims with citations (β, p-value, sample size, replication hash).
       - [ ] Failures rendered with same weight as successes — never buried.
       - [ ] No marketing superlatives without measurable backing.
       - [ ] Both es-CO and en copy reviewed by a native speaker.

       ## Phase 1 review (stub homepage)
       - [ ] `messages/es-CO/lab.json` — reviewed by: ___ on: ___
       - [ ] `messages/en/lab.json`    — reviewed by: ___ on: ___
       ```

    4. Validate YAML: `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))"` — must parse.

    5. After committing, push to a feature branch and open a PR — observe the workflow run. Confirm:
       - lint, typecheck, test-unit, impeccable jobs run on the PR.
       - test-e2e, a11y, lighthouse jobs run when Vercel preview deploys (deployment_status: success).
       - All jobs are required for merge (configure in GitHub repo branch protection settings; user manual setup).
  </action>
  <verify>
    <automated>node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))"</automated>
    Plus structural:
    - `test -f .github/workflows/ci.yml`
    - `grep -c "runs-on: ubuntu-latest" .github/workflows/ci.yml` returns at least 7
    - `grep -q "biome check" .github/workflows/ci.yml`
    - `grep -q "tsc --noEmit" .github/workflows/ci.yml`
    - `grep -q "playwright test" .github/workflows/ci.yml`
    - `grep -q "lhci autorun" .github/workflows/ci.yml`
    - `grep -q "impeccable detect" .github/workflows/ci.yml`
    - `grep -q "deployment_status" .github/workflows/ci.yml`
    - `test -f docs/a11y-audit.md && test -f docs/copy-review.md`
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists with at least 7 distinct jobs (lint, typecheck, test-unit, impeccable, test-e2e, a11y, lighthouse).
    - YAML parses without errors.
    - `test-e2e`, `a11y`, `lighthouse` are conditioned on `deployment_status` event and `state == 'success'`.
    - Impeccable job uses the live-verified flag from docs/impeccable-flag.md (or exit-code-only fallback).
    - `docs/a11y-audit.md` exists with top-5-templates checklist (Phase 1 row filled).
    - `docs/copy-review.md` exists with banned-phrases list and Phase 1 sign-off slots.
    - Maps to 01-VALIDATION.md FOUND-07, FOUND-08, FOUND-09, CROSS-01, CROSS-03, CROSS-04 rows.
  </acceptance_criteria>
  <done>CI workflow committed with 7 parallel jobs; manual audit checklists in place; YAML valid.</done>
</task>

</tasks>

<verification>
- `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'))"` exits 0.
- `pnpm vitest run tests/visual/anti-patterns.spec.ts` exits 0 (impeccable detects planted anti-patterns).
- `pnpm test:a11y` exits 0 against local dev server.
- `pnpm test:lighthouse` exits 0 (or produces a passing run) against local dev server.
- `npx impeccable detect app/ <VERIFIED_FLAG>` exits 0 against the real homepage.
- After Plan 08 is merged: open a PR and confirm all 7 CI jobs run + are required.
</verification>

<success_criteria>
- All FOUND-07 acceptance criteria met: impeccable runs in CI on every PR with verified flag.
- All FOUND-08 acceptance criteria met: Lighthouse CI enforces LCP < 2.5s on Moto G Power 3G.
- All FOUND-09 acceptance criteria met: axe-core scans every PR preview with WCAG 2.2 AA tags.
- All CROSS-01 acceptance criteria met: axe-core CI + manual screen-reader audit checklist.
- All CROSS-03 acceptance criteria met: LCP budget enforced (same as FOUND-08).
- All CROSS-04 acceptance criteria met: impeccable detect with planted-pattern test proving the detector fires.
- Manual audit checklists exist for CROSS-01 manual half and CROSS-10 manual half (sign-off pending native speaker / copy author).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-scaffold/01-08-SUMMARY.md` documenting:
- The live-verified impeccable flag name (or fallback strategy).
- The full CI job matrix and which jobs trigger on which events.
- Lighthouse Moto G Power emulation values + budget thresholds.
- Manual checklists' Phase 1 sign-off status (es-CO native speaker review, copy author review).
- Branch protection setup required by user (each job marked required for merge in repo settings).
- Any open issues blocking CROSS-01 manual or CROSS-10 manual sign-off.
</output>
