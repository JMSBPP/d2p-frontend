import { expect, test } from '@playwright/test'

test.fixme(
  'language switcher persists locale across reload via cookie',
  async ({ page, context }) => {
    // Plan 03 Task 3 fills in the real assertions.
    await page.goto('/')
    void context
    void expect
  },
)

test.fixme('language switcher is keyboard-navigable', async ({ page }) => {
  // Plan 03 Task 3 fills in the real assertions.
  await page.goto('/')
})
